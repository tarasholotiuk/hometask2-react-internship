import React from "react";

function InfoCalculate() {
  // function InfoCalculate({category, arr}) {
  return (
    <div className="item">
      {/* <div className="note-category">{category}</div>
      <div className="active">
				{arr.filter(el => el.isArchived === false && el.category ===
        category).length}
      </div>
      <div className="archived">
				{arr.filter(el => el.isArchived === true && el.category ===
        category).length}
      </div> */}
    </div>
  );
}

export default InfoCalculate;
